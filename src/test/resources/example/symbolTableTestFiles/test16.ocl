package example.symbolTableTestFiles;

import example.CDs.MontiArc.*;
import example.CDs.DefaultTypes.*;

ocl test16 {
  context Cmp cmp inv test16:
    let
        numberUnit = 0 W;
        number = 0.4;
        cmp2 = cmp;
        ports = cmp.ports;

    in
        forall p3 in ports :
                true;
}


