package example.symbolTableTestFiles;

import de.monticore.montiarc.symboltable.MontiArc.*;
import example.DefaultTypes.*;

ocl test16 {
  context Cmp cmp inv test15:
    let
        numberUnit = 0 W;
        number = 0.4;
        cmp2 = cmp;
        ports = cmp.ports;
        ports2 = List{Port p | p in ports};
        ports3 = List{p2 | p2 in ports};
    in
        forall p3 in ports :
                true;
}


