package example.symbolTableTestFiles;

import example.CDs.MontiArc.*;
import example.CDs.DefaultTypes.*;

ocl test17 {
  context Cmp inv test17:
    let
        s = ports.size;
        p = ports;
        b = this.ports.containsAll(p);
        b2 = ports.containsAll(p);
    in
        s > 0;
}


